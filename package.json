{
  "name": "tempo",
  "version": "0.1.0",
  "description": "Agent memory that knows when things stopped being true",
  "main": "dist/index.js",
  "scripts": {
    "test": "npm run build && vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.4"
  },
  "bin": {
    "tempo-mcp": "dist/mcp.js"
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ]
}
